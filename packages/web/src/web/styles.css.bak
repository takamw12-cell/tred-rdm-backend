@import "tailwindcss";
@import "tw-animate-css";
@import "katex/dist/katex.min.css";
@import "@xyflow/react/dist/style.css";

@custom-variant dark (&:is(.dark *));

/* ═══════════════════════════════════════════════════════════════════════
   TRED — Design System
   ───────────────────────────────────────────────────────────────────────
   Bildwelt: das annotierte Vorlesungsskript. Papier, Graphit, Textmarker.
   Die Oberfläche verhält sich wie eine technische Zeichnung: dünne Linien,
   knappe Beschriftungen in Mono, viel Weißraum, wenig Dekoration.

   Farbrollen
     primary    Tiefes Ingenieurgrün — alle Aktionen und Schaltflächen.
     signature  Signalgelb. Der Textmarker: Fortschritt, Treffer, Hervor-
                hebung. Nie als Textfarbe, immer als Fläche unter Text.
     ink        Graphit statt Schwarz — Bleistift ist nie reines Schwarz.

   Der Dunkelmodus ist eigenständig entworfen (Zeichentisch bei Nacht),
   nicht invertiert: das Papier wird zu Schiefer, das Gelb bleibt Gelb.
   ═══════════════════════════════════════════════════════════════════════ */

@theme inline {
  --font-sans: "IBM Plex Sans", ui-sans-serif, system-ui, sans-serif;
  --font-display: "IBM Plex Sans Condensed", "IBM Plex Sans", ui-sans-serif, sans-serif;
  --font-mono: "IBM Plex Mono", ui-monospace, SFMono-Regular, monospace;

  /* Technische Zeichnungen haben keine weichen Ecken. Radien bleiben klein
     und abgestuft; die Kachel des App-Icons ist die einzige Ausnahme. */
  --radius-sm: calc(var(--radius) - 3px);
  --radius-md: calc(var(--radius) - 1px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);

  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-signature: var(--signature);
  --color-signature-foreground: var(--signature-foreground);
  --color-mastered: var(--mastered);
  --color-learning: var(--learning);
  --color-new: var(--new);
  --color-chart-1: var(--chart-1);
  --color-chart-2: var(--chart-2);
  --color-chart-3: var(--chart-3);
  --color-chart-4: var(--chart-4);
  --color-chart-5: var(--chart-5);
  --color-sidebar: var(--sidebar);
  --color-sidebar-foreground: var(--sidebar-foreground);
  --color-sidebar-primary: var(--sidebar-primary);
  --color-sidebar-primary-foreground: var(--sidebar-primary-foreground);
  --color-sidebar-accent: var(--sidebar-accent);
  --color-sidebar-accent-foreground: var(--sidebar-accent-foreground);
  --color-sidebar-border: var(--sidebar-border);
  --color-sidebar-ring: var(--sidebar-ring);
}

/* ── Hell: Papier ──────────────────────────────────────────────────────── */
:root {
  --radius: 0.5rem;

  --background: #f4f5f2;
  --foreground: #14181b;
  --card: #ffffff;
  --card-foreground: #14181b;
  --popover: #ffffff;
  --popover-foreground: #14181b;

  --primary: #0f4f49;
  --primary-foreground: #f7f8f5;
  --signature: #f2b705;
  --signature-foreground: #14181b;

  --secondary: #eaece7;
  --secondary-foreground: #2a3035;
  --muted: #eaece7;
  --muted-foreground: #5a6169;
  --accent: #e3e7e2;
  --accent-foreground: #14181b;
  --destructive: #b3261e;

  --border: #dfe2dd;
  --input: #d5d9d3;
  --ring: #0f4f49;

  /* Wissensstände: erarbeitet / in Arbeit / unberührt */
  --mastered: #0f7a5a;
  --learning: #f2b705;
  --new: #9aa3a8;

  --chart-1: #0f4f49;
  --chart-2: #3f8f83;
  --chart-3: #f2b705;
  --chart-4: #b3261e;
  --chart-5: #5a6169;

  --sidebar: #eff1ed;
  --sidebar-foreground: #14181b;
  --sidebar-primary: #0f4f49;
  --sidebar-primary-foreground: #f7f8f5;
  --sidebar-accent: #e3e7e2;
  --sidebar-accent-foreground: #14181b;
  --sidebar-border: #dfe2dd;
  --sidebar-ring: #0f4f49;

  /* Millimeterpapier: Rasterfarbe und Teilung */
  --grid-line: rgb(20 24 27 / 0.05);
  --grid-step: 28px;
}

/* ── Dunkel: Zeichentisch bei Nacht ────────────────────────────────────── */
.dark {
  --background: #0e1214;
  --foreground: #e8ebe6;
  --card: #161b1e;
  --card-foreground: #e8ebe6;
  --popover: #161b1e;
  --popover-foreground: #e8ebe6;

  /* Auf Schiefer trägt das tiefe Grün nicht mehr — es wird zu Leuchtgrün,
     damit Schaltflächen den Kontrast für weiße Schrift behalten. */
  --primary: #3fbfa8;
  --primary-foreground: #06201d;
  --signature: #ffc93c;
  --signature-foreground: #14181b;

  --secondary: #1f262a;
  --secondary-foreground: #e8ebe6;
  --muted: #1f262a;
  --muted-foreground: #97a0a4;
  --accent: #253036;
  --accent-foreground: #e8ebe6;
  --destructive: #ef5350;

  --border: rgb(232 235 230 / 0.12);
  --input: rgb(232 235 230 / 0.18);
  --ring: #3fbfa8;

  --mastered: #4fc38f;
  --learning: #ffc93c;
  --new: #6b757a;

  --chart-1: #3fbfa8;
  --chart-2: #7fd8c6;
  --chart-3: #ffc93c;
  --chart-4: #ef5350;
  --chart-5: #97a0a4;

  --sidebar: #11161a;
  --sidebar-foreground: #e8ebe6;
  --sidebar-primary: #3fbfa8;
  --sidebar-primary-foreground: #06201d;
  --sidebar-accent: #253036;
  --sidebar-accent-foreground: #e8ebe6;
  --sidebar-border: rgb(232 235 230 / 0.12);
  --sidebar-ring: #3fbfa8;

  --grid-line: rgb(232 235 230 / 0.045);
}

@layer base {
  * {
    @apply border-border outline-ring/50;
  }
  body {
    @apply bg-background text-foreground;
    font-family: var(--font-sans);
    -webkit-font-smoothing: antialiased;
    text-rendering: optimizeLegibility;
  }
  h1, h2, h3, h4 {
    font-family: var(--font-display);
    letter-spacing: -0.015em;
    font-weight: 600;
  }
}

@layer utilities {
  .font-display { font-family: var(--font-display); }
  .font-mono { font-family: var(--font-mono); }

  /* Beschriftung im Zeichnungsstil: Mono, versal, weit gesperrt. Trägt
     Schrittnummern, Seitenangaben und Statuszeilen. */
  .label-tech {
    font-family: var(--font-mono);
    font-size: 0.6875rem;
    line-height: 1.2;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted-foreground);
  }

  /* `brand-gradient` heißt aus Gründen der Abwärtskompatibilität weiter so —
     an rund zwanzig Stellen im Code. Es ist jetzt eine ruhige Vollfläche
     statt eines Verlaufs; die Marke trägt keine Verläufe mehr. */
  .brand-gradient {
    background-color: var(--primary);
    color: var(--primary-foreground);
  }

  /* Der Textmarker: Gelb liegt unter der Zeile, nicht in der Schrift. */
  .brand-gradient-text {
    position: relative;
    color: inherit;
    background-image: linear-gradient(var(--signature), var(--signature));
    background-repeat: no-repeat;
    background-size: 100% 0.42em;
    background-position: 0 88%;
    padding-inline: 0.12em;
  }

  /* Millimeterpapier als Untergrund — nur für großflächige, leere Bereiche
     (Anmeldung, leere Zustände). Nie hinter Fließtext. */
  .paper-grid {
    background-image:
      linear-gradient(to right, var(--grid-line) 1px, transparent 1px),
      linear-gradient(to bottom, var(--grid-line) 1px, transparent 1px);
    background-size: var(--grid-step) var(--grid-step);
  }

  /* Haarlinie im Zeichnungssinn: immer genau ein Gerätepixel. */
  .rule-hairline { border-top: 1px solid var(--border); }
}

/* React Flow theming */
.react-flow__attribution { display: none; }

/* ── Barrierefreiheit: Schriftgröße der Lesebereiche ────────────────────
   Skaliert nur Flächen mit `.reading-scalable` (Antworten, Wörterbuch,
   Übungen). Layout, Seitenleisten und Diagramme bleiben unberührt.
   KaTeX rechnet in em, Formeln wachsen also mit. Unlayered Regeln
   gewinnen gegen Tailwinds `text-sm`. */
:root { --reading-font-size: 16px; }
html[data-font-size="small"]  { --reading-font-size: 16px; }
html[data-font-size="medium"] { --reading-font-size: 20px; }
html[data-font-size="large"]  { --reading-font-size: 24px; }
.reading-scalable {
  font-size: var(--reading-font-size);
  line-height: 1.65;
}
.reading-scalable :is(code, pre) { font-size: 0.85em; }

/* Bewegung ist Beiwerk — wer sie abbestellt, bekommt sie nicht. */
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
